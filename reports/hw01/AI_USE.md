# AI Use — Homework 1

## 1. What I used an AI assistant for, and what I did myself

I used AI to help with layout CSS so the web page looks good as it was my first time doing CSS. I also used it to draft Docker / agents run commands in the README.

I also used AI to help me get a better understanding of the homework requirements konwing what exactly I should work on.

## 2. One AI-produced output that was wrong or unsuitable
SS added unused classes (`.eyebrow`, `.intro`), a card layout that did not match a simple homework page, and a leftover `</p>` in `web_app.html`.

## 3. How I detected the problem or verified the result

I compared the HTML, CSS, and Python files with the assignment instruction. Unused CSS showed up because those classes were never in the HTML.

## 4. What I changed and why it works now

I deleted the unused CSS and extra success-message UI. These features were generated by AI, and were not related to our project. 
